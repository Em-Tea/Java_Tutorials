package VariableScope;

public class Main {
    public static void main(String[] args) {
        // local variables are declared inside a method and visible only to that method
        //global variables are declared outside of a method, but within a class visible to all parts of a class

        DiceRoller diceRoll = new DiceRoller();
    }
}
